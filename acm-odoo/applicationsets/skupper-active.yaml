---
# Deploys Skupper Site (interior router) on the active cluster.
# This cluster acts as the VAN endpoint that exposes the PostgreSQL
# primary service to the passive cluster.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: odoo-skupper-active
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/part-of: odoo-dr
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - clusterDecisionResource:
        configMapRef: acm-placement
        labelSelector:
          matchLabels:
            cluster.open-cluster-management.io/placement: odoo-active
        requeueAfterSeconds: 180
  template:
    metadata:
      name: 'odoo-skupper-{{.name}}'
      labels:
        app.kubernetes.io/part-of: odoo-dr
        odoo.io/component: skupper
    spec:
      project: default
      source:
        repoURL: https://github.com/ryannix123/acm-odoo.git
        targetRevision: main
        path: clusters/active/skupper
      destination:
        name: '{{.name}}'
        namespace: odoo
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - ServerSideApply=true
