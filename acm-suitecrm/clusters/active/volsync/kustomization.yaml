apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: suitecrm

resources:
  - replication-source.yaml

# Note: The volsync-restic-config secret containing S3 credentials must be
# created out-of-band via External Secrets Operator, Sealed Secrets, or
# Vault integration. Do NOT commit S3 credentials to Git.
