---
# ManagedClusterSet groups the active and passive clusters together.
# Clusters are added to this set by setting the label:
#   cluster.open-cluster-management.io/clusterset=odoo-dr
apiVersion: cluster.open-cluster-management.io/v1beta2
kind: ManagedClusterSet
metadata:
  name: odoo-dr
  labels:
    app.kubernetes.io/part-of: odoo-dr
spec:
  clusterSelector:
    selectorType: ExclusiveClusterSetLabel
---
# Bind the ManagedClusterSet to the odoo-acm namespace so Placements
# in this namespace can select clusters from it.
apiVersion: cluster.open-cluster-management.io/v1beta2
kind: ManagedClusterSetBinding
metadata:
  name: odoo-dr
  namespace: odoo-acm
spec:
  clusterSet: odoo-dr
---
# Also bind to openshift-gitops so ApplicationSets there can use the same set
apiVersion: cluster.open-cluster-management.io/v1beta2
kind: ManagedClusterSetBinding
metadata:
  name: odoo-dr
  namespace: openshift-gitops
spec:
  clusterSet: odoo-dr
