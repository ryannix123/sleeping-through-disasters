---
# ACM Policy that monitors PostgreSQL replication health on the standby cluster.
# Reports non-compliance if the standby is not in replica mode or not connected.
apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: odoo-replication-healthy
  namespace: odoo-acm
  labels:
    app.kubernetes.io/part-of: odoo-dr
  annotations:
    policy.open-cluster-management.io/categories: SI System and Information Integrity
    policy.open-cluster-management.io/controls: SI-13 Predictable Failure Prevention
    policy.open-cluster-management.io/standards: NIST SP 800-53
spec:
  remediationAction: inform
  disabled: false
  policy-templates:
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: standby-cluster-in-replica-mode
        spec:
          remediationAction: inform
          severity: high
          object-templates:
            - complianceType: musthave
              objectDefinition:
                apiVersion: postgresql.cnpg.io/v1
                kind: Cluster
                metadata:
                  name: odoo-db
                  namespace: odoo
                spec:
                  replica:
                    enabled: true
---
# Bind ONLY to passive clusters - this policy doesn't apply to the active.
apiVersion: policy.open-cluster-management.io/v1
kind: PlacementBinding
metadata:
  name: odoo-replication-healthy-binding
  namespace: odoo-acm
  labels:
    app.kubernetes.io/part-of: odoo-dr
placementRef:
  name: odoo-policy-passive-placement
  apiGroup: cluster.open-cluster-management.io
  kind: Placement
subjects:
  - name: odoo-replication-healthy
    apiGroup: policy.open-cluster-management.io
    kind: Policy
---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: odoo-policy-passive-placement
  namespace: odoo-acm
  labels:
    app.kubernetes.io/part-of: odoo-dr
spec:
  clusterSets:
    - odoo-dr
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchLabels:
            role: passive
