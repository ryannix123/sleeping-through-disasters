---
# Selects the cluster labelled role=active.
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: odoo-active
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/part-of: odoo-dr
spec:
  clusterSets:
    - odoo-dr
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchLabels:
            role: active
