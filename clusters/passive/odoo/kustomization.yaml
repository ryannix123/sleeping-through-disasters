apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: odoo
resources:
  - odoo.yaml

# The odoo-admin Secret (admin-password, login-password) is NOT in Git.
# Create it out of band with Sealed Secrets, External Secrets Operator, or
# imperatively during bootstrap. See docs/BOOTSTRAP.md.
