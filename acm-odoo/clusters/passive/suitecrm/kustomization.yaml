apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: suitecrm

resources:
  - configmap.yaml
  - redis.yaml
  - suitecrm.yaml
  # Note: scheduler.yaml is NOT deployed on passive - it would conflict
  # with the active cluster's scheduler running against the same database
  # via streaming replication.
